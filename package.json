{
  "name": "cyber-training",
  "version": "1.0.0",
  "description": "Quiz CyberCitizen",
  "main": "app.js",
  "scripts": {
    "build:js": "terser app.js -o dist/app.js -c -m",
    "build:css": "postcss styles/*.css --dir dist/styles/ --base styles/",
    "build:html": "cp index.html dist/",
    "build:data": "node -e \"require('fs').readdirSync('data').forEach(f => { if (f.endsWith('.json')) { const c = require('./data/' + f); require('fs').mkdirSync('dist/data', {recursive:true}); require('fs').writeFileSync('dist/data/' + f, JSON.stringify(c)); } })\"",
    "build:assets": "npm run build:data && cp -r media dist/ && cp -r favicon dist/",
    "build": "mkdir -p dist/styles && npm run build:js && npm run build:css && npm run build:html && npm run build:assets"
  },
  "keywords": [],
  "author": "TolteK-Damicell",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^7.1.9",
    "postcss": "^8.5.16",
    "postcss-cli": "^11.0.1",
    "terser": "^5.48.0"
  }
}
